x = c(1:10)
y = c(1:100)
z = c(1:1000)

# Somme des inverses au carré de 1 à 1000
sum(1/x^2)
sum(1/y^2)
sum(1/z^2)

# Ecart avec la valeur théorique
pi^2/6 - sum(1/z^2)

# Loi uniforme
X = c(1:10)^2

# Esperance de X
mean(n)

# Variance de X
var(n)
